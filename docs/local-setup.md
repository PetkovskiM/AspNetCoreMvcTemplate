
---

## `docs/local-setup.md`

```md
# Local Setup

This document explains how to run the template locally.

## Prerequisites

- .NET SDK installed
- SQL Server available locally
- Visual Studio or another editor
- EF Core tools if you want to run migrations from CLI

Example EF Core tools install:

```bash
dotnet tool install --global dotnet-ef